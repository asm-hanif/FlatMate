(function(){'use strict';
const box=document.getElementById('dashboardContainer'); if(!box)return;
const esc=v=>String(v??'').replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;').replace(/'/g,'&#039;');
async function load(){try{const s=await(await fetch('/api/auth/session',{credentials:'include'})).json();if(!s.authenticated){box.innerHTML='<div class="owner-empty-state"><h3>Please Sign In</h3><p>Sign in to manage your FlatMate activity.</p><a class="btn btn-primary" href="/login.html">Sign In</a></div>';return;}const role=s.user.role;const [favRes,reqRes]=await Promise.all([fetch('/api/favorites',{credentials:'include'}),fetch('/api/requests/mine',{credentials:'include'})]);const fav=await favRes.json();const req=await reqRes.json();const isOwner=['Owner','Both'].includes(role);const isSeeker=['User','Both'].includes(role);box.innerHTML=`<div class="section-header"><span class="section-eyebrow">MY FLATMATE</span><h1 class="section-title">Welcome back, ${esc(s.user.name||'there')}</h1><p>Your home-search activity and account shortcuts in one place.</p></div><div class="owner-dashboard-stats"><div class="owner-stat-card"><div class="owner-stat-icon"><i class="far fa-heart"></i></div><div><span class="owner-stat-number">${isSeeker?(fav.favorites?.length||0):0}</span><span class="owner-stat-label">Saved Properties</span></div></div><div class="owner-stat-card"><div class="owner-stat-icon"><i class="far fa-paper-plane"></i></div><div><span class="owner-stat-number">${isSeeker?(Array.isArray(req)?req.length:0):0}</span><span class="owner-stat-label">My Requests</span></div></div><div class="owner-stat-card"><div class="owner-stat-icon"><i class="fas fa-user-tag"></i></div><div><span class="owner-stat-number">${esc(role==='Both'?'Both':role==='Owner'?'Owner':'Seeker')}</span><span class="owner-stat-label">Account Mode</span></div></div></div><div class="profile-section"><h3>Quick Actions</h3><div style="display:flex;gap:.75rem;flex-wrap:wrap"><a class="btn btn-primary" href="/flats.html"><i class="fas fa-search"></i> Find a Home</a>${isOwner?'<a class="btn btn-outline" href="/owner-dashboard.html"><i class="fas fa-house"></i> Manage My Properties</a>':''}<a class="btn btn-outline" href="/profile.html"><i class="fas fa-user"></i> Profile & Settings</a><a class="btn btn-outline" href="/chat.html"><i class="fas fa-comments"></i> Messages</a></div></div>${isSeeker?`<div class="profile-section"><h3>Recent Requests</h3>${Array.isArray(req)&&req.length?req.slice(0,5).map(r=>`<div class="request-item"><div><strong>${esc(r.Type)}</strong> · ${esc(r.FlatTitle)}<br><small>${esc(r.OwnerName||'Owner')}</small></div><span class="request-status status-${esc(String(r.Status||'').toLowerCase())}">${esc(r.Status)}</span></div>`).join(''):'<p>No requests yet. <a href="/flats.html">Explore properties</a> to get started.</p>'}</div>`:''}`;}catch(e){console.error(e);box.innerHTML='<div class="owner-empty-state"><h3>Could not load dashboard</h3><p>Please refresh and try again.</p></div>';}}
load();
})();
