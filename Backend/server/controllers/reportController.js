const {executeSql,sql}=require('../db');
const reasons=new Set(['Fake property','Scam or suspicious behavior','Wrong information','Duplicate listing','Already rented/sold','Inappropriate content','Other']);
async function reportFlat(req,res){const flatId=Number(req.params.id), userId=Number(req.session.userId), reason=String(req.body.reason||'').trim(), details=String(req.body.details||'').trim().slice(0,2000); if(!Number.isInteger(flatId)||!reasons.has(reason)) return res.status(400).json({success:false,error:'Choose a valid report reason.'}); try{const f=await executeSql(`SELECT Id,OwnerId FROM Flats WHERE Id=@id`,[{name:'id',type:sql.Int,value:flatId}]);if(!f.length)return res.status(404).json({success:false,error:'Property not found.'});if(Number(f[0].OwnerId)===userId)return res.status(400).json({success:false,error:'You cannot report your own property.'});const dup=await executeSql(`SELECT Id FROM PropertyReports WHERE FlatId=@fid AND ReporterId=@uid AND Status='Open' LIMIT 1`,[{name:'fid',type:sql.Int,value:flatId},{name:'uid',type:sql.Int,value:userId}]);if(dup.length)return res.status(409).json({success:false,error:'You already reported this property.'});await executeSql(`INSERT INTO PropertyReports(FlatId,ReporterId,Reason,Details) VALUES(@fid,@uid,@reason,@details)`,[{name:'fid',type:sql.Int,value:flatId},{name:'uid',type:sql.Int,value:userId},{name:'reason',type:sql.NVarChar(100),value:reason},{name:'details',type:sql.NVarChar(2000),value:details||null}]);res.status(201).json({success:true,message:'Thanks. Your report has been submitted.'});}catch(e){console.error(e);res.status(500).json({success:false,error:'Could not submit report.'});}}
module.exports={reportFlat};